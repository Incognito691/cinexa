/**
 * Public API of the continue-watching feature.
 *
 * Currently just the home-page rail, which still renders placeholder rows —
 * real progress data arrives with the persistence layer.
 */
export { ContinueWatchingRail } from "./components/continue-watching-rail";
