/**
 * Public API of the AI feature.
 *
 * Nothing is wired up yet — the page is an empty state. The chat itself will
 * run on the existing OPENAI_API_KEY, following the fetch pattern already in
 * `server/filter/layers/layer-9-ai.ts`.
 */
export { AiComingSoon } from "./components/ai-coming-soon";
export { AuroraBackdrop } from "./components/aurora-backdrop";
